{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "transpile": "julia {{CONVERT_SCRIPT}} --project-root . --all-scripts",
    "transpile:engine": "julia {{CONVERT_SCRIPT}}",
    "build:wasm": "npm run build:wasm --prefix {{JULGAME_TS_DIR}}"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
